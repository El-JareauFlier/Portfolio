import Layout from "../components/Layout";

const NoPage = () => {
    return ( 
        //if the page is not found, this will be displayed
        <Layout>
            <h1>404, page not found</h1> 
        </Layout>
    );
}
 
export default NoPage;
